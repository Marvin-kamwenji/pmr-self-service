import React from 'react'

/**
 * @author Mesh
 * @function SegmentSeparator just playing with components
 * @param {border} param0 
 * @returns just a line with margin
 */
export default function SegmentSeparator({border}) {
  return (
    <div className='h-5 w-full mt-4'>
      <hr/>
    </div>
  )
}
